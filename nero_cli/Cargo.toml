[package]
name = "nero_cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "nero"
path = "src/main.rs"

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
nero_core = { path = "../nero_core" }
tokio = { version = "1", features = ["rt", "macros", "rt-multi-thread"] }
