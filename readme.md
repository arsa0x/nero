# Nero

> A lightweight DSL and CLI tool for defining and executing HTTP requests.

Nero is a developer-friendly tool that allows you to write HTTP requests using a custom scripting language (`.ns`) and execute them directly from the terminal.

It is designed to be fast, minimal, and extensible — with future support for compilation and LSP (Language Server Protocol).

---
